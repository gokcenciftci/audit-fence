# Contributing to AuditFence

Thank you for considering contributing to AuditFence!

## Development Setup

1. Clone the repository.
2. Install dependencies: `npm install`
3. Run tests: `npm test`
4. Run full validation: `npm run validate`
